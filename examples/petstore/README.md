# Petstore API MCP Server

> Auto-generated by [MCP Toolbox](https://github.com/YOUR_USERNAME/mcp-toolbox)

## Quick Start

```bash
# Install dependencies
pip install -e .

# Set environment variables
export PETSTORE_API_BASE_URL="https://api.petstore.example.com/v1"
export PETSTORE_API_API_KEY="your-api-key"

# Optional reliability tuning
export PETSTORE_API_RETRY_MAX_ATTEMPTS="2"
export PETSTORE_API_RETRY_BACKOFF_SECONDS="0.5"
export PETSTORE_API_RETRY_UNSAFE_METHODS="false"

# Run the server
petstore_api-mcp-server
```

## Available Tools

| Tool | Method | Path | Description |
|------|--------|------|-------------|
| `list_pets` | GET | `/pets` | List pets |
| `get_pet_by_id` | GET | `/pets/{petId}` | Get pet by ID |
| `list_orders` | GET | `/store/orders` | List store orders |
| `create_pet` | POST | `/pets` | Create a pet |
| `update_pet` | PUT | `/pets/{petId}` | Update an existing pet |
| `delete_pet` | DELETE | `/pets/{petId}` | Delete pet by ID |


## Pagination Hints

The generator detected pagination patterns for these tools:

| Tool | Pattern |
|------|---------|
| `list_pets` | `offset_limit` |


Use `get_pagination_hints()` from the generated server module for a programmatic map.

## Configuration

| Variable | Description | Required |
|----------|-------------|----------|
| `PETSTORE_API_BASE_URL` | API base URL | Yes |
| `PETSTORE_API_TIMEOUT_CONNECT_SECONDS` | Connect timeout (seconds) | No |
| `PETSTORE_API_TIMEOUT_READ_SECONDS` | Read timeout (seconds) | No |
| `PETSTORE_API_TIMEOUT_WRITE_SECONDS` | Write timeout (seconds) | No |
| `PETSTORE_API_TIMEOUT_POOL_SECONDS` | Pool timeout (seconds) | No |
| `PETSTORE_API_RETRY_MAX_ATTEMPTS` | Retry attempts for retryable failures | No |
| `PETSTORE_API_RETRY_BACKOFF_SECONDS` | Backoff base seconds (exponential) | No |
| `PETSTORE_API_RETRY_UNSAFE_METHODS` | Allow retries for non-idempotent methods (POST/PUT/PATCH) | No |
| `PETSTORE_API_API_KEY` | API key | Yes |


## Use with Claude Desktop

Add to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "petstore_api": {
      "command": "petstore_api-mcp-server",
      "env": {
        "PETSTORE_API_BASE_URL": "https://api.petstore.example.com/v1",
        "PETSTORE_API_API_KEY": "your-api-key"
      }
    }
  }
}
```
